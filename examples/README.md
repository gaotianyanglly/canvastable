# Canvas Table 示例

本目录包含 Canvas Table 的各种示例，展示不同的功能和用法。

---

## 📋 示例列表

### 1. dynamic-style-example.html
**动态样式配置示例**

展示如何实时修改表格样式，包括：
- 行高调整
- 字体大小调整
- 颜色配置（文字、边框、背景等）
- 样式重置

**适用场景：**
- 主题切换
- 用户自定义外观
- 实时样式预览

---

### 2. performance-test.html
**性能测试页面**

用于测试和监控表格性能，包括：
- 大数据量测试（100 - 100,000 行）
- FPS 实时监控
- 渲染性能统计
- 压力测试

**适用场景：**
- 性能测试
- 性能优化验证
- 大数据量场景测试

---

## 🚀 如何运行示例

### 方法 1：直接打开 HTML 文件

1. 确保已经构建项目：
   ```bash
   npm run build
   ```

2. 使用浏览器直接打开示例文件：
   - `examples/dynamic-style-example.html`
   - `examples/performance-test.html`

### 方法 2：使用本地服务器（推荐）

使用本地服务器可以避免跨域问题：

```bash
# 使用 Python
python -m http.server 8000

# 或使用 Node.js http-server
npx http-server -p 8000

# 或使用 VS Code Live Server 插件
```

然后在浏览器中访问：
- http://localhost:8000/examples/dynamic-style-example.html
- http://localhost:8000/examples/performance-test.html

---

## 📁 文件说明

### 构建输出

项目构建后会生成以下文件：

```
umd/
└── canvastable.min.js    # UMD 格式，用于浏览器

dist/
├── index.js              # CommonJS 格式，用于 Node.js
├── index.d.ts            # TypeScript 类型定义
└── ...                   # 其他模块文件
```

**示例文件使用：** `umd/canvastable.min.js`

---

## 🎯 示例详解

### 动态样式配置示例

```javascript
// 创建表格
const table = new CanvasTable({
  container: document.getElementById('table-container'),
  columns: [...],
  dataSource: [...],
  style: {
    width: '100%',
    height: 600
  }
});

// 修改单个样式
table.updateStyle('rowHeight', 50);

// 批量修改样式
table.updateStyles({
  rowHeight: 50,
  fontSize: '16px',
  textColor: '#333'
});

// 重置样式
table.resetStyle();
```

### 性能测试示例

```javascript
// 创建大数据量表格
const table = new CanvasTable({
  container: document.getElementById('container'),
  columns: [...],
  dataSource: generateData(10000), // 10000 行数据
  style: {
    width: '100%',
    height: 600
  }
});

// 配置渲染性能
table.renderManager.setMinRenderInterval(16); // 60fps

// 配置离屏 Canvas 缓存
table.offscreenManager.setMaxCacheSize(50);
```

---

## 🔧 常见问题

### Q: 打开示例页面显示空白？

**A:** 确保已经运行 `npm run build` 构建项目，生成 `umd/canvastable.min.js` 文件。

### Q: 控制台报错 "CanvasTable is not defined"？

**A:** 检查 HTML 文件中的 script 标签路径是否正确：
```html
<script src="../umd/canvastable.min.js"></script>
```

### Q: 控制台报错 "require is not defined"？

**A:** 这说明使用了错误的构建文件。应该使用 `umd/canvastable.min.js` 而不是 `dist/index.js`。

### Q: 性能测试页面 FPS 很低？

**A:** 
1. 检查数据量是否过大
2. 尝试调整渲染间隔：`table.renderManager.setMinRenderInterval(33)` (30fps)
3. 检查浏览器性能

---

## 📊 性能基准

在现代浏览器中的预期性能：

| 数据量 | 初始渲染 | 滚动 FPS | 内存占用 |
|--------|----------|----------|----------|
| 1,000 行 | < 100ms | 60 | ~10MB |
| 10,000 行 | < 500ms | 60 | ~50MB |
| 50,000 行 | < 2s | 50-60 | ~200MB |

**测试环境：**
- Chrome 120+
- CPU: Intel i5 或更高
- 内存: 8GB 或更高

---

## 💡 提示

1. **使用本地服务器**
   - 避免跨域问题
   - 更接近生产环境

2. **打开开发者工具**
   - 查看控制台日志
   - 监控性能指标
   - 调试问题

3. **尝试不同配置**
   - 调整数据量
   - 修改样式配置
   - 测试不同场景

---

## 📚 相关文档

- [快速开始指南](../docs/QUICK_START.md)
- [API 参考文档](../docs/API_REFERENCE.md)
- [优化总结](../docs/OPTIMIZATION_SUMMARY.md)

---

## 🤝 贡献

欢迎贡献更多示例！

如果你有好的示例想法，可以：
1. Fork 项目
2. 添加新的示例文件
3. 提交 Pull Request

---

**祝你使用愉快！** 🎉

